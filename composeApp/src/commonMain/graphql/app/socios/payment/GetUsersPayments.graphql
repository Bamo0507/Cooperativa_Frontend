query GetUsersPayments($accessToken: String!) {
    getUsersPayments(accessToken: $accessToken) {
        id
        name
        commentary
        paymentDate
        state
        ticketNum
        photoPath
        totalAmount
        accountNum
    }
}